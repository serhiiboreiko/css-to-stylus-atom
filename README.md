# css-to-stylus-atom
Atom package which convert your css to stylus.

# Regex

`/[ ]*{/g` - find `{` or ` {` or `   {` etc


`/:[ ]{1,}/g` - find `: ` or `:  ` etc


`/:/g` - find alone `:`


`/;/g` - find alone `;`


`/}/g` - find `}`
